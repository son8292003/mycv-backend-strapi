{
  "routes": [
    {
      "method": "GET",
      "path": "/site-data",
      "handler": "site-data.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/site-data",
      "handler": "site-data.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/site-data",
      "handler": "site-data.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
